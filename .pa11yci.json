{
  "defaults": {
    "standard": "WCAG2AA",
    "timeout": 30000,
    "wait": 1000,
    "chromeLaunchConfig": {
      "args": ["--no-sandbox", "--disable-setuid-sandbox"]
    },
    "hideElements": "span.flag, #map, .hero-eyebrow-text, section:first-of-type .eyebrow, #app_for_home"
  }
}
